#include <stdio.h>
/**
 * main - input single digit of base 10 from 0
 *
 * Return: always 0
 *
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
